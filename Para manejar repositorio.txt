antes de poner estos comandos abrir en la carpeta donde se guardara el proyecto
y seleccionas dentro y presionas el boton izquierdo y  abres GIT BASH HERE

Para descargar del repositorio por primera ves:
git clone https://github.com/francogq/TF_COMPLEJIDAD

Para subir al repositorio alguna modificacion:
git status

git add nombre del archivo rojo

git status

git commit -m "mensaje"

git push origin master


Para descargar las modificaciones por otro integrante:

git pull